# Runtime dependencies beyond ComfyUI's own requirements (numpy, av, tokenizers,
# safetensors, huggingface_hub, Pillow, scipy, aiohttp are provided by ComfyUI).
# Keep in sync with [project].dependencies in pyproject.toml.
#
# Nothing extra is needed. Optional features declare their own install
# command in the error they raise (see docs/dev/coding-standards.md); for example
# mutagen (GPL-2.0, embeds cover art) is optional and never installed by Plenio.
