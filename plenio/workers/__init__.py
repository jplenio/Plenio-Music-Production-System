"""Worker entry points, each run as ``python -m plenio.workers.<name> --job <folder>``.

The protocol lives in ``plenio.core.workers``. The first real worker (lyrics
ASR) arrives with the YuE2 Cover path (Phase 4B).
"""
